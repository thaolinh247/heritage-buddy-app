import {
  View as RNView,
  Text as RNText,
  Pressable as RNPressable,
  Image as RNImage,
  type ViewProps,
  type TextProps,
  type PressableProps,
  type ImageProps,
} from "react-native";

export const View = RNView;
export const Text = RNText;
export const Pressable = RNPressable;
export const Image = RNImage;

export type { ViewProps, TextProps, PressableProps, ImageProps };
